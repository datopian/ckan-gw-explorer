# react-scripts@5.0.1 ships stale peerOptional ranges (e.g. typescript ^3||^4,
# react <19) that don't reflect what actually works. Use legacy peer resolution
# so the monorepo can run TypeScript 5 + React 19 without --force on every install.
legacy-peer-deps=true
